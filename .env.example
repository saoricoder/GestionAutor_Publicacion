# Docker / Compose variables (copy to .env and edit if needed)

# MySQL (Authors DB)
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=db_authors
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_PORT=3307

# PostgreSQL (Publications DB)
POSTGRES_DB=db_publications
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
POSTGRES_PORT=5433

# Service ports
AUTHORS_PORT=8000
PUBLICATIONS_PORT=8080
FRONTEND_PORT=5173

# Frontend Vite env
VITE_APP_NAME=GestionAut_Pub

# Optional JVM opts
JAVA_OPTS=
